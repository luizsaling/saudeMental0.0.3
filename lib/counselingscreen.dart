import 'package:flutter/material.dart';

class Counselingscreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Virtual Counseling'),
      ),
      body: Center(
        child: Text('Chat interface here'),
      ),
    );
  }
}
